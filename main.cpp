#include <iostream>
#include <vector>
#include <string>
#include <iomanip>

using namespace std;

// Structure to hold component data (Tests, Assignments, etc.)
struct Component {
    string name;
    double weight;
    double score;
    double maxScore;
    double contribution;
};
int main(){
    return 0;
}